{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Ginno desktop shell",
  "windows": ["main", "pin"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
