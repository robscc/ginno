// Workspace content (ChatStream, TopBar, RightPanel, SheetViewer) is rendered
// inside AppShell so it stays mounted across route changes — preserving WS
// connections, in-memory message store, and error-card retry state.
export default function WorkspacePage() {
  return null;
}
